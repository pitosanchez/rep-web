{
  "name": "rep",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix rep-web run dev",
    "build": "npm --prefix rep-web run build",
    "start": "npm --prefix rep-web run start",
    "lint": "npm --prefix rep-web run lint",
    "lint:fix": "npm --prefix rep-web run lint:fix",
    "type-check": "npm --prefix rep-web run type-check",
    "pipeline": "npm --prefix rep-web run pipeline",
    "pipeline:download": "npm --prefix rep-web run pipeline:download",
    "pipeline:spatial-join": "npm --prefix rep-web run pipeline:spatial-join",
    "pipeline:cluster": "npm --prefix rep-web run pipeline:cluster"
  }
}

